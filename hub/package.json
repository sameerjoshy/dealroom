{
  "name": "hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "node build.mjs && wrangler pages deploy dist --project-name dealroom --branch main"
  }
}
